// Calculation / model version for persisted and exported insights.
// Bump this when any calculation logic (PK model, weight trend, adherence,
// BMI, etc.) changes so historical exports remain distinguishable.
export const CALCULATION_VERSION = "2.0.0";
export const PRIVACY_POLICY_VERSION = "1.0.0";