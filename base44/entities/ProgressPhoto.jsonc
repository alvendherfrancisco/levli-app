{
  "type": "object",
  "properties": {
    "day_key": {
      "type": "string",
      "description": "YYYY-MM-DD"
    },
    "url": {
      "type": "string"
    }
  },
  "required": [
    "day_key",
    "url"
  ],
  "name": "ProgressPhoto",
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": null,
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}